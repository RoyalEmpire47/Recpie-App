<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firebasev1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReportedRecipes"></activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchResult" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".RecipeDisplayActivity" />
        <activity android:name=".ShowCountryWiseRecipeActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".AddRecipeSubmitActivity" />
        <activity android:name=".AddIngredientListActivity" />
        <activity android:name=".AddRecipeActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>